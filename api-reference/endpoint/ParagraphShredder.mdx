---
title: "Paragraph Shredder"
api: "POST https://api-v2.longshot.ai/custom/api/content/paragraph/shredder"
description: "This endpoint allows you to shred or trim a paragraph. Provides shorter version of the paragraph with the complete meaning associated."
---

### Headers

<ParamField header="AUTHORIZATION" type="string" placeholder="Bearer <Secret Token>" required>
  Add the Authorization Bearer Token to authorize your api request.
</ParamField>

### Body

<ParamField body="paragraph" type="string" required>
  This field is used as input to provide an paragraph.
  
  Example: 
  "Multi-tenancy is an architecture pattern in which a single instance of a software application serves multiple tenants (customers or users) independently, while maintaining their data and configurations isolated from one another.
  This approach allows multiple tenants to share resources such as compute, storage, and network infrastructure, which can lead to improved cost efficiency, ease of maintenance, and scalability for the service provider."
</ParamField>

### Response

<ResponseField name="status" type="string">
  Indicates whether the call was successful. success if successful, otherwise if not.
</ResponseField>

<ResponseField name="copies" type="object">

The contents of the copies

<Expandable title="Toggle object">

<ResponseField name="content" type="string">
  This is the shredded paragraph content.
</ResponseField>

</Expandable>

</ResponseField>

<ResponseField name="message" type="string">
  This is the information attached to the call. This is useful when any error occurs, it provides more meaningful information about it. 
</ResponseField>

<ResponseField name="credits_used" type="number">
  This field informs you about the credits used for this particular request. It varies upon the generated content.
</ResponseField>

<RequestExample>

```bash Example Request
curl --location --request POST 'https://api-v2.longshot.ai/custom/api/generate/paragraph/shredder' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer <token>' \
-H 'Content-Type: application/json' \
--data-raw '{
    "paragraph": ""
}'
```

</RequestExample>

<ResponseExample>

```json Response
{
	"copies": [
		{
			"content": "<Generated Shredded Paragraph>"
		}
	],
	"status": "success",
	"message": "Generated successfully",
	"credits_used": <Credits>
}
```

</ResponseExample>
